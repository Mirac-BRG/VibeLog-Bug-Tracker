version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app.db:/app/app.db
    environment:
      - SECRET_KEY=${SECRET_KEY:-dev-secret-key-fallback}
      - FLASK_ENV=production
    restart: unless-stopped
